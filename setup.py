from setuptools import setup, find_packages

setup(
    name='bond_yield_surface_construction',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # List your project dependencies here
        # 'numpy',
        # 'pandas',
        # 'scipy',
        # ...
    ],
    # Add other package data, entry points, classifiers...
)
